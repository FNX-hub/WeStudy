/*
 * This file was generated by the Gradle 'init' task.
 *
 * This generated file contains a sample Java Library project to get you started.
 * For more details take a look at the Java Libraries chapter in the Gradle
 * User Manual available at https://docs.gradle.org/6.3/userguide/java_library_plugin.html
 */

plugins {
    // Apply the java-library plugin to add support for Java Library
    id 'java-library'
  	id 'application'
  	id 'org.openjfx.javafxplugin' version '0.0.8'
  	id 'war'
  	id "org.gretty" version "3.0.3"
  	id "jacoco"
  	id "org.sonarqube" version "3.0"
}

javafx {
    version = "14"
    modules = [ 'javafx.controls', 'javafx.fxml']
}

sonarqube {
  properties {
    property "sonar.projectKey", "FNX-hub_WeStudy"
  }
}

jacoco {
    toolVersion = "0.8.5"
    reportsDir = file("$buildDir/customJacocoReportDir")
}

jacocoTestReport {
    reports {
        xml.enabled true
        csv.enabled false
        html.destination file("${buildDir}/jacocoHtml")
    }
}

test {
    jacoco {
        destinationFile = file("$buildDir/jacoco/jacocoTest.exec")
        classDumpDir = file("$buildDir/jacoco/classpathdumps")
    }
}

mainClassName = 'logic.control.AppLauncher'

repositories {
    // Use jcenter and mavenCentral for resolving dependencies.
    jcenter()
    mavenCentral()
}

dependencies {
    // This dependency is exported to consumers, that is to say found on their compile classpath.
    api 'org.apache.commons:commons-math3:3.6.1'

    // This dependency is used internally, and not exposed to consumers on their own compile classpath.
    implementation 'com.google.guava:guava:28.2-jre'

    // Use JUnit test framework
    testImplementation 'junit:junit:4.12'
    
    // JDBC Dependency
    // https://mvnrepository.com/artifact/com.oracle.database.jdbc/ojdbc10
	//compile group: 'com.oracle.database.jdbc', name: 'ojdbc10', version: '19.8.0.0'
	
	// https://mvnrepository.com/artifact/com.mysql.jdbc/org.motechproject.com.mysql.jdbc
	//compile group: 'com.mysql.jdbc', name: 'org.motechproject.com.mysql.jdbc', version: '5.1.32-r028'
	
    
    //implementation 'com.oracle.database.jdbc:ojdbc8:19.6.0.0'
    //implementation 'com.oracle.database.jdbc:ucp:19.6.0.0'    
}
